# Copy to research/disc_assistant/assistant/.env (already ignored by the repository).
# Set a private bootstrap admin key, for example using: openssl rand -hex 32
# An empty value deliberately prevents Compose from starting.
TYPESENSE_API_KEY=
TYPESENSE_PORT=8108
