# Optional comparison environment only; ordinary Assistant dependencies are unchanged.
# Direct pins, not a cross-platform transitive lock. Setup saves pip freeze locally.
-r ../assistant/requirements.txt
sherpa-onnx==1.13.8
numpy==2.2.6
certifi==2026.7.22
