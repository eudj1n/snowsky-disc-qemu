# Copy to .env and set the path to the firmware OTA chunk directory
# (the `main_os/ota_v240` folder of the unzipped V2.40 firmware — see firmware/README.md).
# Used by compose.yaml (and written for you by `./run.sh up <dir>`).
OTA_DIR=/absolute/path/to/SNOWSKY_DISC_update_.../main_os/ota_v240
